{
  "name": "gol-scraper",
  "description": "Scraper de autos VW Gol/Gol Trend usados en AMBA con scoring IA",
  "scripts": {
    "setup": "python3 -m venv .venv && source .venv/bin/activate && pip install -r requirements.txt && mkdir -p data logs && ln -sf \"$CONDUCTOR_ROOT_PATH/.env\" .env",
    "run": "source .venv/bin/activate && python runner.py",
    "archive": "rm -rf .venv __pycache__ scrapers/__pycache__ scoring/__pycache__ converter/__pycache__"
  }
}
